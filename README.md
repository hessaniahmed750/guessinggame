# Guessing Game Project

This is a simple shell script project that implements a number guessing game.
The user must guess the number of files in the current directory.

## 🚀 Execution

To run the game, navigate to the project directory in your terminal and execute the script:

```bash
./guessinggame.sh
